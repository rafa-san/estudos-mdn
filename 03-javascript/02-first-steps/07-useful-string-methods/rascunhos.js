



console.log(finalQuote);










/*

let teste = quote.toLowerCase() + quote.at(0).toUpperCase();
console.log(teste);


// Corrigir a frase para "sentence case" (primeira letra maiúscula, restante minúsculo)
let fixedQuote = quote.toLowerCase();
fixedQuote = fixedQuote.at(0).toUpperCase() + fixedQuote.slice(1);

// Substituir "green eggs and ham" por outro alimento
fixedQuote = fixedQuote.replace('green eggs and ham', 'broccoli and spinach');

// Adicionar o ponto final
if (fixedQuote.endsWith('.')) {
  console.log(fixedQuote)
} else {
  console.log(`${fixedQuote}.`)
}

console.log(fixedQuote);


*/



//const finalQuote = fixedQuote.endsWith('.') ? fixedQuote : fixedQuote + '.';


//const fixedQuote = `${quote[0]} ${quote.toLowerCase().slice(2).replace("green eggs and ham", "chicken and coca-cola")}.`;

//console.log(fixedQuote);


/*
const fixedQuote = `${quote.toLowerCase().replace("i", "I").replace("green eggs and ham", "chicken and coca-cola")}.`;

console.log(fixedQuote);


`${quote[0]}`



quote[0] + quote.slice(1).toLowerCase();
*/
